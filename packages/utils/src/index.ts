/**
 * @cloudwerk/utils
 *
 * Shared browser-safe utilities for Cloudwerk.
 * This package has no Node.js dependencies and can be safely
 * imported in both server and browser code.
 *
 * @packageDocumentation
 */

export { serializeProps, deserializeProps } from './serialization.js'
export { escapeHtmlAttribute } from './html.js'
