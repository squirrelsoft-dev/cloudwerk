{
  "name": "cloudwerk",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @cloudwerk/cli dev",
    "build": "turbo run build --filter='!./examples/*'",
    "test": "turbo run test --filter='!./examples/*'",
    "test:e2e": "vitest --config e2e/vitest.config.ts --run",
    "validate:spike": "./scripts/validate-spike.sh",
    "lint": "eslint . --ignore-pattern 'examples/**'",
    "format": "prettier --write .",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter='!./examples/*' && changeset publish --no-git-tag",
    "release:provenance": "turbo run build --filter='!./examples/*' && pnpm -r publish --access public --provenance --no-git-checks"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.31.1",
    "@eslint/js": "^8.57.0",
    "@types/node": "^20",
    "eslint": "^8",
    "prettier": "^3.9.6",
    "turbo": "^2.10.6",
    "typescript": "^5",
    "typescript-eslint": "^7.0.0",
    "vitest": "^1.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.28.0"
    }
  }
}
