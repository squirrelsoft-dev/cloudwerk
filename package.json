{
  "name": "cloudwerk",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @cloudwerk/cli dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "eslint": "^8",
    "prettier": "^3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
